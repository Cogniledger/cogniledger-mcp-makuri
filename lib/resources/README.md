# Resources

Reserved for static markdown resources exposed via MCP `resources/list` and `resources/read` in a future version.

v1 does not advertise any MCP resources; all metadata is exposed exclusively through the eight tools. This directory exists to make the v2 extension point obvious without introducing a dependency now.
