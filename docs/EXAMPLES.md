# Client transcripts

Real transcripts and screenshots are added by Leonid after the production deploy. This file is the placeholder structure.

## Claude Desktop transcript

> _To be filled in after the production deploy. Suggested flow: ask Claude "What is Makuri?" then "What's its compliance posture?" — capture the responses with tool-call inspector turned on._

```text
[user] What is Makuri?
[claude calls] get_platform_info
[claude] ...
```

## Le Chat (Mistral) transcript

> _To be filled in after the production deploy. Suggested flow: ask "Which languages does Makuri support, and is Arabic among them?" — exercises the `locale` filter._

## Cursor transcript

> _To be filled in after the production deploy. Suggested flow: ask the agent "Use the Makuri MCP server to summarize the platform's tech stack" — verifies the connector is reachable from a coding-focused client._

## ChatGPT Apps SDK / other clients

> _Add transcripts as integrations are tested._
